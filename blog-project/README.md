# Inkwell — Blog Project

A modern React blog UI (React 19 + Vite + Tailwind CSS 4).

## Run locally

```bash
npm install
npm run dev
```

Then open the address shown in the terminal (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```
